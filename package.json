{
  "name": "web-mobile-ecommerce-all",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack web & mobile e-commerce project configured for Render.com deployment",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
